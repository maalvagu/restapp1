<%= form_for(@dpto) do |f| %>
  <% if @dpto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dpto.errors.count, "error") %> prohibited this dpto from being saved:</h2>

      <ul>
      <% @dpto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
